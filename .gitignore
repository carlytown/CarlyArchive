node_modules/
.env
.env.local
scripts/.cache/
.DS_Store
*.log

# Build artefacts — regenerated on every CI deploy from Notion + APIs.
# Locally, run `npm run build` to populate them.
data/
assets/covers/
assets/lastfm/
